# 連絡帳

作業内容の概要をこちらに時刻付きで記録していきます。

- 2026-03-07 19:22 JST
  - リポジトリをローカルに clone: `kurehajime/hdr_sandbox`
  - ユーザー提供成功例 `IMG_0892.PNG` を解析
    - 400x400 / PNG / 16bit / ColorType 6(RGBA)
    - iCCPあり、ICC内 `cicp=[9,16,0,1]`（BT.2020 + PQ）を確認
  - 解析結果を `docs/success-case-IMG_0892.md` に記録
  - 事前検証用スクリプト `scripts/check_png_hdr.py` を追加
    - 指定PNGのIHDR・iCCP・cicpを抽出
    - 今回の成功プロファイル一致判定(`match_target_profile`)を表示

- 2026-03-07 19:38 JST
  - 再現候補生成スクリプト `scripts/make_candidates.py` を追加
    - 参照成功PNGからICC(iCCP)を抽出
    - 400x400正規化した入力から4候補を生成
      - success_like (16bit RGBA + iCCP/cicp)
      - fail_8bit
      - fail_no_iccp
      - fail_rgb_no_alpha
    - `generated/comparison.md` に比較結果を自動出力
  - 生成物と手順を `docs/reproduction-candidates.md` に記録
  - READMEに再現候補一括生成コマンドを追記

- 2026-03-07 19:47 JST
    - 人間による追記
        - `docs/reproduction-candidates.md` に実際にXで投稿した結果を追記
- 2026-03-07 19:52 JST
  - 人間検証結果を反映して仮説を更新
    - `docs/hypothesis-update-2026-03-07.md` を新規作成
      - 旧仮説（16bit/alpha必須）の反証点を整理
      - 更新仮説（iCCP/cicp主導）と次実験の判定基準を明記
  - `scripts/make_candidates.py` を拡張
    - `--extended` 追加
    - iCCP/cicpを維持したまま非iCCP条件を変える4候補を生成可能化
      - `candidate_probe_8bit_rgb_no_alpha.png`
      - `candidate_probe_alpha_255.png`
      - `candidate_probe_alpha_0.png`
      - `candidate_probe_size_512.png`
    - 比較レポートを legacy / relaxed の2判定併記に変更
  - `python3 scripts/make_candidates.py --extended` を実行して `generated/comparison.md` を更新
  - `docs/reproduction-candidates.md` / `README.md` を第2版相当に更新
- 2026-03-07 20:20 JST
    - 人間による追記
        - `docs/hypothesis-update-2026-03-07.md` に実際にXで投稿した結果を追記

- 2026-03-07 20:24 JST
  - 白化観測（`alpha=0` / `size=512`）の解釈を反映
    - `docs/hypothesis-update-2026-03-07.md`
      - 白化を「iCCP/cicp反証」ではなく「透明度条件で評価不能の可能性」として整理
      - 次実験を白化回避プローブ中心に更新
    - `docs/reproduction-candidates.md`
      - 第3版に更新し、白化回避プローブ（alpha=1 / alphaグラデーション / 512+非透明）を追記
  - `scripts/make_candidates.py --extended` を拡張
    - 追加生成候補
      - `candidate_probe_alpha_1.png`
      - `candidate_probe_alpha_gradient.png`
      - `candidate_probe_size_512_nontransparent.png`
    - `generated/comparison.md` のextended説明も更新
  - 上記スクリプト実行により `generated/` を再生成（11候補）

- 2026-03-07 20:34 JST
    - 人間による追記
        - `docs/hypothesis-update-2026-03-07.md` に実際にXで投稿した結果を追記

- 2026-03-07 20:43 JST
  - 最新の人間検証結果（`alpha_gradient`右だけ光る / `alpha_1`白化 / `size_512_nontransparent`黒化）を反映して整理
    - `docs/hypothesis-update-2026-03-07.md`
      - 仮説を「iCCP/cicp単独」から「透明度 × 実効輝度 × 表示しきい値」へ更新
      - 右半分のみ発光観測をしきい値境界の示唆として明記
    - `docs/reproduction-candidates.md`
      - 第4版に更新
      - 新規追加プローブ（alpha=16, alpha=64, 左右分割16/64, 512+alpha255+高輝度パッチ）を追記
  - `scripts/make_candidates.py --extended` を最小追加拡張
    - 追加生成候補
      - `candidate_probe_alpha_16.png`
      - `candidate_probe_alpha_64.png`
      - `candidate_probe_alpha_lr_split_16_64.png`
      - `candidate_probe_size_512_alpha255_bright_patch.png`
    - 比較説明（`generated/comparison.md` 末尾）を更新
  - `python3 scripts/make_candidates.py --extended` を再実行
    - `generated/comparison.md` を15候補構成に更新

- 2026-03-07 21:37 JST
  - alphaしきい値の観測を1枚で行うため、alphaラダープローブを追加
    - `scripts/make_candidates.py`
      - `candidate_probe_alpha_ladder_1_255.png` を新規生成
      - alpha lane仕様（1,2,4,8,16,24,32,48,64,96,128,192,255）を `generated/alpha_ladder_spec.md` として自動出力
      - `generated/comparison.md` のextended説明にalphaラダー目的を追記
  - `python3 scripts/make_candidates.py --extended` を実行し、16候補を再生成
  - `python3 scripts/check_png_hdr.py generated/candidate_probe_alpha_ladder_1_255.png` で iCCP/cicp 維持を確認
  - ドキュメント更新
    - `README.md` 出力一覧にalphaラダー関連ファイルを反映
    - `docs/reproduction-candidates.md` を第5版へ更新（alphaラダー観測方針を追加）
    - `docs/hypothesis-update-2026-03-07.md` にalphaラダー実験を追加
- 2026-03-07 21:56 JST
    - 人間による追記
        - `docs/hypothesis-update-2026-03-07.md` に実際にXで投稿した結果を追記

- 2026-03-07 22:01 JST
  - 実験設計を「alpha条件を固定した実効輝度しきい値測定」に拡張
    - 目的: alpha依存とRGB依存を分離し、しきい値laneのシフトを観測
  - `scripts/make_candidates.py --extended` を拡張
    - 新規生成候補
      - `candidate_probe_luma_ladder_alpha255.png`（RGB段階バー、alpha=255固定）
      - `candidate_probe_luma_ladder_alpha64.png`（RGB段階バー、alpha=64固定）
    - `generated/luma_ladder_spec.md`（laneごとのRGB値と実効輝度目安）を自動出力
    - `generated/comparison.md` のextended説明にlumaラダーの狙いを追記
  - `python3 scripts/make_candidates.py --extended` を実行し、18候補を再生成
  - `python3 scripts/check_png_hdr.py` で2つのlumaラダー候補とも iCCP/cicp 維持（`match_target_profile: YES`）を確認
  - ドキュメント更新
    - `README.md` にlumaラダー生成物を追記
    - `docs/reproduction-candidates.md` を第6版へ更新
    - `docs/hypothesis-update-2026-03-07.md` にlumaラダー実験を追加

- 2026-03-07 22:33 JST
  - 実験設計を「alpha×luma 2Dマトリクスによる境界形状観測」に拡張
    - 目的: しきい値境界が `effective ≈ (alpha/255)*(luma/65535)` の等高線に沿うかを1枚で検証
  - `scripts/make_candidates.py --extended` を拡張
    - 新規生成候補
      - `candidate_probe_alpha_luma_matrix.png`
    - 仕様書自動出力
      - `generated/alpha_luma_matrix_spec.md`
    - `generated/comparison.md` のextended説明に2Dマトリクス実験を追記
  - `python3 scripts/make_candidates.py --extended` を実行し、19候補を再生成
  - `python3 scripts/check_png_hdr.py generated/candidate_probe_alpha_luma_matrix.png` で iCCP/cicp 維持（`match_target_profile: YES`）を確認
  - ドキュメント更新
    - `README.md` に2Dマトリクス生成物を反映
    - `docs/reproduction-candidates.md` を第7版へ更新
    - `docs/hypothesis-update-2026-03-07.md` に2Dマトリクス仮説を追記

- 2026-03-07 22:31 JST
  - 30分イテレーションとして「alpha×luma 2D境界観測」プローブを追加
    - 仮説: 発光境界は `effective ≈ (alpha/255) * (luma/65535)` に近い等高線として現れる
  - `scripts/make_candidates.py --extended` を拡張
    - 新規生成候補
      - `candidate_probe_alpha_luma_matrix.png`（x=alpha, y=luma の2Dグリッド）
    - 新規仕様書
      - `generated/alpha_luma_matrix_spec.md`
    - `generated/comparison.md` のextended説明に2Dマトリクス目的を追記
  - `python3 scripts/make_candidates.py --extended` を再実行（19候補）
  - `python3 scripts/check_png_hdr.py generated/candidate_probe_alpha_luma_matrix.png` で
    `match_target_profile: YES` を確認
  - ドキュメント更新
    - `docs/reproduction-candidates.md` を第7版へ更新
    - `docs/hypothesis-update-2026-03-07.md` に2D境界観測の実験意図を追記
    - `README.md` 出力一覧に2Dマトリクス関連ファイルを追記

- 2026-03-07 23:14 JST
  - 実験設計を「しきい値近傍の局所拡大（threshold zoom）」へ拡張
    - 目的: alphaラダーで示唆された境界（alpha≈24付近）を高密度2Dサンプリングで追跡
  - `scripts/make_candidates.py --extended` を拡張
    - 新規生成候補
      - `candidate_probe_threshold_zoom_matrix.png`
    - 新規仕様書
      - `generated/threshold_zoom_matrix_spec.md`
    - `generated/comparison.md` のextended説明にthreshold zoom matrixを追記
  - `python3 scripts/make_candidates.py --extended` を実行し、21候補を再生成
  - `python3 scripts/check_png_hdr.py generated/candidate_probe_threshold_zoom_matrix.png` で iCCP/cicp 維持（`match_target_profile: YES`）を確認
  - ドキュメント更新
    - `README.md` に新規生成物と仕様書を反映
    - `docs/reproduction-candidates.md` を第9版へ更新
    - `docs/hypothesis-update-2026-03-07.md` にthreshold zoom仮説を追記

- 2026-03-07 23:47 JST
  - 人間追記の運用を `docs/human-observations.md` に分離
    - 既存のX投稿観測（11件）を移植し、`observed` 値を正規化（glows/not_glows/whiteout/blackout/mixed）
    - 追記ルールを明文化（同一candidate再投稿は時系列で追加）
  - `scripts/make_candidates.py --extended` を更新
    - `generated/human_observations_template.md` を自動出力する処理を追加
  - 新規チェックツール `scripts/check_human_observations.py` を追加
    - 観測テーブルの必須列・`observed` 値・生成物ファイル存在を検証
  - 検証実行
    - `python3 scripts/make_candidates.py --extended`（25候補生成 + template出力）
    - `python3 scripts/check_human_observations.py --observations docs/human-observations.md --generated-dir generated`（OK）
    - `python3 scripts/check_png_hdr.py generated/candidate_probe_threshold_zoom_matrix.png`（`match_target_profile: YES`）
  - ドキュメント更新
    - `README.md` に human-observations 運用とチェックコマンドを追記
    - `docs/reproduction-candidates.md` を第10版に更新
    - `docs/hypothesis-update-2026-03-07.md` に「観測追記先分離」メモを追記

- 2026-03-08 00:03 JST
  - 人間観測ログ（`docs/human-observations.md`）を入力に、cicp要素の切り分け実験を6条件へ拡張
    - 背景: `fail_no_iccp=not_glows` と `alpha_gradient=mixed` から「cicp要素の寄与」を独立検証
  - `scripts/make_candidates.py --extended` を更新
    - cicp比較候補を2件追加
      - `candidate_probe_cicp_bt2020_pq_limited.png`（range:1→0）
      - `candidate_probe_cicp_bt2020_pq_matrix_1.png`（matrix:0→1）
    - `generated/cicp_variant_spec.md` を6条件前提に更新
    - `generated/comparison.md` のextended説明を更新
  - `docs/human-observations.md` に新規2候補を `observed=todo` で追記
  - 検証実行
    - `python3 scripts/make_candidates.py --extended`（27候補生成 + template更新）
    - `python3 scripts/check_human_observations.py --observations docs/human-observations.md --generated-dir generated`（OK）
    - `python3 scripts/check_png_hdr.py generated/candidate_probe_cicp_bt2020_pq_limited.png`（cicp=[9,16,0,0]）
    - `python3 scripts/check_png_hdr.py generated/candidate_probe_cicp_bt2020_pq_matrix_1.png`（cicp=[9,16,1,1]）
  - ドキュメント更新
    - `README.md` に新規候補2件を追加
    - `docs/reproduction-candidates.md` を第11版に更新

- 2026-03-08 00:14 JST
  - 実験設計を「観測テーブルの網羅性担保 + 次投稿バッチの自動抽出」に拡張
    - 背景: generatedには存在するが `docs/human-observations.md` に未登録の候補が10件あり、比較計画が分断されていた
  - `scripts/check_human_observations.py` を拡張
    - 追加機能
      - `pending_rows`（未投稿/URL未反映）の集計と一覧出力
      - `missing_in_table`（generatedに存在するが観測表未登録）の検出
      - `--report-out` でMarkdownレポートを自動生成
      - `--strict-pending` で pending があれば終了コード2
  - `docs/human-observations.md` を更新
    - 未登録だった10候補を `observed=todo` で追記し、表の網羅率を100%化
    - observed凡例に `todo` を追加
  - 検証実行
    - `python3 scripts/check_human_observations.py --observations docs/human-observations.md --generated-dir generated --report-out docs/observation-status-2026-03-08.md`（OK, rows=27, missing_in_table=0）
    - `python3 scripts/check_human_observations.py --observations docs/human-observations.md --generated-dir generated --strict-pending`（exit=2を確認）
  - ドキュメント更新
    - `README.md` に `--report-out` / `--strict-pending` の利用方法を追記
    - `docs/reproduction-candidates.md` を第12版へ更新
    - `docs/observation-status-2026-03-08.md` を生成（cicp 6条件を最優先バッチとして抽出）

- 2026-03-08 00:16 JST
  - `git pull --rebase --autostash` の取り込み競合を解消
    - upstreamで追加された `probe_threshold_zoom_matrix` の人間観測URLを維持
    - candidate名を `probe_threshold_zoom_matrix` に正規化し、observedを `not_glows` として反映
  - `python3 scripts/check_human_observations.py --report-out ...` を再実行
    - `rows=27`, `missing_in_table=0`, `pending_rows=19` を確認

- 2026-03-08 00:48 JST
  - 実験設計を「未投稿管理」から「再現性管理」へ拡張
    - 背景: 同一candidateの再投稿が増える見込みのため、pending管理だけでは判定品質が不足
  - `scripts/check_human_observations.py` を拡張
    - 追加機能
      - `conflicting_candidates`（同一candidateで `glows/not_glows/mixed` が衝突）を検出
      - `retry_candidates`（最新が `whiteout/blackout/mixed`）を自動抽出
      - `--strict-conflict` を追加（衝突があれば終了コード2）
      - レポートに conflict/retry セクションを追加
  - 検証実行
    - `python3 scripts/check_human_observations.py --report-out docs/observation-status-2026-03-08.md`（OK, `conflicting_candidates=0`, `retry_candidates=5`）
    - `python3 scripts/check_human_observations.py --strict-conflict`（OK）
    - `python3 scripts/check_human_observations.py --strict-pending`（exit=2を維持）
  - ドキュメント更新
    - `README.md` に `conflicting_candidates` / `retry_candidates` / `--strict-conflict` を追記
    - `docs/reproduction-candidates.md` を第13版へ更新
    - `docs/observation-status-2026-03-08.md` を再生成

- 2026-03-08 01:54 JST
  - 実験設計を「観測ログの分割運用耐性」に拡張
    - 背景: 人間観測を主テーブル + 補助メモに分けても、機械チェックで一括集計したい
  - `scripts/check_human_observations.py` を拡張
    - `--observations-glob` を追加し、`--observations` の親ディレクトリ配下で複数Markdownを同時読込可能化
    - `observation_files` を出力し、実際に読み込んだファイルを明示
    - glob未一致時にエラー終了して取りこぼしを早期検出
  - 補助ドキュメントを追加
    - `docs/human-observations-extra.md`（分割運用時のテンプレート）
  - 検証実行
    - `python3 scripts/check_human_observations.py --observations docs/human-observations.md --generated-dir generated`（OK）
    - `python3 scripts/check_human_observations.py --observations docs/human-observations.md --observations-glob 'human-observations-*.md' --generated-dir generated`（OK）
    - `python3 scripts/check_human_observations.py --observations docs/human-observations.md --observations-glob 'human-observations-*.md' --generated-dir generated --report-out docs/observation-status-2026-03-08.md --batch-size 10`（OK）
    - `python3 -m py_compile scripts/check_human_observations.py`（OK）
  - ドキュメント更新
    - `README.md` に `--observations-glob` の使い方と `observation_files` 出力を追記
    - `docs/reproduction-candidates.md` を第15版へ更新

- 2026-03-08 02:18 JST
  - 実験設計を「次バッチ提案の情報密度向上」に拡張
    - 背景: `docs/human-observations.md` 上で family ごとの進捗偏り（cicp/luma/size の未完了）が大きく、優先順リストだけでは投稿戦略を切り替えにくい
  - `scripts/check_human_observations.py` を拡張
    - 追加機能
      - `family_progress_latest` をCLI出力（family別の `resolved / uncertain / todo_or_url_todo / completion`）
      - レポートに `Family progress (latest per candidate)` テーブルを追加
      - `Suggested immediate batch` に `1b) 多様性重視バッチ`（family round-robin）を追加
        - 同一family連投を避け、端末ドリフト疑い時の比較効率を向上
  - 検証実行
    - `python3 -m py_compile scripts/check_human_observations.py`（OK）
    - `python3 scripts/check_human_observations.py --observations docs/human-observations.md --generated-dir generated --report-out docs/observation-status-2026-03-08.md --batch-size 10`（OK）
      - `rows=27`, `pending_rows=19`, `missing_in_table=0`, `family_progress_latest` 出力を確認
      - レポートに family進捗表と 1b多様性バッチが出力されることを確認
  - ドキュメント更新
    - `README.md` に `family_progress_latest` / `1b) 多様性重視バッチ` を追記
    - `docs/reproduction-candidates.md` を第16版へ更新
    - `docs/observation-status-2026-03-08.md` を再生成

- 2026-03-08 02:24 JST
  - 実験設計を「投稿計画の外部自動化連携」に拡張
    - 背景: Markdownレポートは人間閲覧向けで、bot/スクリプト連携時に再パースが必要だった
  - `scripts/check_human_observations.py` を拡張
    - 追加機能
      - `--report-json-out` を追加し、機械処理向けJSONを直接出力
      - JSONに `summary / family_progress / pending / retry / conflicts / suggested_batches` を格納
      - `suggested_batches` は `priority / diversified_round_robin / cicp_focus` を同梱
  - 検証実行
    - `python3 -m py_compile scripts/check_human_observations.py`（OK）
    - `python3 scripts/check_human_observations.py --observations docs/human-observations.md --observations-glob 'human-observations-*.md' --generated-dir generated --report-out docs/observation-status-2026-03-08.md --report-json-out docs/observation-status-2026-03-08.json --batch-size 10`（OK）
      - `report_json_written` を確認
      - JSONに pending 19件・retry 5件・cicp_focus 6件が入ることを確認
  - ドキュメント更新
    - `README.md` に `--report-json-out` を追記
    - `docs/reproduction-candidates.md` を第17版へ更新
    - `docs/observation-status-2026-03-08.json` を生成

- 2026-03-08 03:13 JST
  - 30分イテレーションとして「左右位置バイアス vs alphaしきい値」の切り分けプローブを追加
    - `scripts/make_candidates.py --extended` に以下を追加
      - `candidate_probe_alpha_gradient_rl.png`（右→左alphaグラデーション）
      - `candidate_probe_alpha_gradient_tb.png`（上→下alphaグラデーション）
      - `generated/alpha_gradient_orientation_spec.md`（向き比較の観測判定ガイド）
    - `generated/comparison.md` のextended説明に2プローブの意図を反映
  - 観測TODOを更新
    - `docs/human-observations.md` に `probe_alpha_gradient_rl` / `probe_alpha_gradient_tb` を `todo` で追加
  - ドキュメント更新
    - `README.md` 出力一覧に2プローブ + orientation specを反映
    - `docs/reproduction-candidates.md` を第19版へ更新（向き差分実験の狙いを追記）
  - 検証実行
    - `python3 scripts/make_candidates.py --extended`（29候補再生成）
    - `python3 scripts/check_human_observations.py --observations docs/human-observations.md --generated-dir generated`（OK）
    - `python3 scripts/check_png_hdr.py generated/candidate_probe_alpha_gradient_rl.png`（match_target_profile: YES）
    - `python3 scripts/check_png_hdr.py generated/candidate_probe_alpha_gradient_tb.png`（match_target_profile: YES）
    - `python3 -m py_compile scripts/*.py`（OK）

- 2026-03-08 03:13 JST
  - 実験設計を「alpha_gradientの向き差分による位置バイアス切り分け」に拡張
    - 背景: `probe_alpha_gradient` で右半分のみ発光（mixed）が観測され、alpha依存か左右固定の位置要因か未分離
  - `scripts/make_candidates.py --extended` を拡張
    - 新規生成候補
      - `candidate_probe_alpha_gradient_rl.png`（右→左グラデーション）
      - `candidate_probe_alpha_gradient_tb.png`（上→下グラデーション）
    - 仕様書自動出力
      - `generated/alpha_gradient_orientation_spec.md`
    - `generated/comparison.md` のextended説明に向き差分プローブを追記
  - `docs/human-observations.md` を更新
    - 新規2候補を `observed=todo` で追記（追記先は human-observations を維持）
  - 検証実行
    - `python3 scripts/make_candidates.py --extended`（29候補を再生成）
    - `python3 -m py_compile scripts/make_candidates.py scripts/check_human_observations.py`（OK）
    - `python3 scripts/check_png_hdr.py generated/candidate_probe_alpha_gradient_rl.png`（`match_target_profile: YES`）
    - `python3 scripts/check_png_hdr.py generated/candidate_probe_alpha_gradient_tb.png`（`match_target_profile: YES`）
    - `python3 scripts/check_human_observations.py --observations docs/human-observations.md --observations-glob 'human-observations-*.md' --generated-dir generated --report-out docs/observation-status-2026-03-08.md --report-json-out docs/observation-status-2026-03-08.json --batch-size 10`（OK, `rows=29`, `pending_rows=21`, `missing_in_table=0`）
  - ドキュメント更新
    - `README.md` に新規候補2件と `alpha_gradient_orientation_spec.md` を追記
    - `docs/reproduction-candidates.md` を第19版へ更新
    - `docs/observation-status-2026-03-08.md` / `.json` を再生成

- 2026-03-08 04:53 JST
  - 実験設計を「投稿チェックリストへの追試パック同梱」に拡張
    - 背景: `Suggested immediate batch` の追試提案はレポート側にのみ出ており、実投稿時に手元チェックリストへ転記が必要だった
  - `scripts/check_human_observations.py` を拡張
    - `--checklist-include-targeted-followups` を追加
    - `--post-checklist-out` 生成物に `1b) Targeted follow-up packs` を任意で追加可能化
    - チェックリスト冒頭メタに `include_targeted_followups` を出力
  - 検証実行
    - `python3 -m py_compile scripts/check_human_observations.py scripts/make_candidates.py scripts/check_png_hdr.py`（OK）
    - `python3 scripts/check_human_observations.py --observations docs/human-observations.md --generated-dir generated --report-out docs/observation-status-2026-03-08.md --report-json-out docs/observation-status-2026-03-08.json --post-checklist-out docs/posting-checklist-2026-03-08.md --checklist-batch-mode diversified --checklist-include-targeted-followups --batch-size 10 --batch-family-cap 2`（OK, `rows=29`, `pending_rows=21`, `missing_in_table=0`）
  - ドキュメント更新
    - `README.md` に `--checklist-include-targeted-followups` の利用例/説明を追加
    - `docs/reproduction-candidates.md` を第21版相当に更新
    - `docs/posting-checklist-2026-03-08.md` を再生成（追試パック入り）

- 2026-03-08 05:28 JST
  - 実験設計を「alpha_gradientの mixed を位置依存かどうか直接検証」に拡張
    - 背景: `probe_alpha_gradient` で右半分のみ発光が継続しており、勾配方向差分だけでは判定が曖昧
  - `scripts/make_candidates.py --extended` を修正
    - 新規生成候補
      - `candidate_probe_position_quadrant_alpha64.png`（4象限に同一パッチを配置）
    - 新規仕様書
      - `generated/position_quadrant_spec.md`
    - `generated/comparison.md` のextended説明に位置依存バイアス検証プローブを追記
  - `scripts/check_human_observations.py` を更新
    - family分類に `position` を追加（進捗集計に反映）
    - `probe_alpha_gradient` が mixed のとき、追試パックに `probe_position_quadrant_alpha64` を自動同梱
  - 検証実行
    - `python3 scripts/make_candidates.py --input sample/success_sample.png --success-ref sample/success_sample.png --outdir generated --extended`（30候補再生成）
    - `python3 scripts/check_png_hdr.py generated/candidate_probe_position_quadrant_alpha64.png`（`match_target_profile: YES`）
    - `python3 scripts/check_human_observations.py --observations docs/human-observations.md --generated-dir generated --report-out docs/observation-status-2026-03-08.md --report-json-out docs/observation-status-2026-03-08.json --post-checklist-out docs/posting-checklist-2026-03-08.md --checklist-batch-mode diversified --checklist-include-targeted-followups --batch-size 10 --batch-family-cap 2 --strict-mapping`（OK, `rows=30`, `pending_rows=22`, `missing_in_table=0`）
    - `python3 -m py_compile scripts/*.py`（OK）
  - ドキュメント更新
    - `docs/observation-status-2026-03-08.md` / `.json` / `docs/posting-checklist-2026-03-08.md` を再生成

- 2026-03-08 05:43 JST
  - 実験設計を「非決定結果の残存をCIで即検知」に拡張
    - 背景: pending=0でも最新結果が `whiteout/blackout/mixed` の候補が残ると再現性が未確定のまま通過してしまう
  - `scripts/check_human_observations.py` を拡張
    - `--strict-retry` を追加
    - 最新観測が `whiteout/blackout/mixed` の候補が1件でもあれば終了コード2
  - 検証実行
    - `python3 -m py_compile scripts/check_human_observations.py`（OK）
    - `python3 scripts/check_human_observations.py --observations docs/human-observations.md --generated-dir generated --report-out docs/observation-status-2026-03-08.md --report-json-out docs/observation-status-2026-03-08.json --post-checklist-out docs/posting-checklist-2026-03-08.md --checklist-batch-mode diversified --checklist-include-targeted-followups --batch-size 10 --batch-family-cap 2 --strict-mapping`（OK, `retry_candidates=5`）
    - `python3 scripts/check_human_observations.py --observations docs/human-observations.md --generated-dir generated --strict-retry`（exit=2を確認）
  - ドキュメント更新
    - `README.md` に `--strict-retry` の説明を追加
    - `docs/reproduction-candidates.md` を第24版に更新

- 2026-03-08 05:44 JST
  - 実験設計を「観測URLの取り違え（同一URLの多重帰属）検知」へ拡張
    - 背景: candidate/file 対応が正しくても、`x_post_url` の貼り間違いがあると再現判定が崩れる
  - `scripts/check_human_observations.py` を拡張
    - `url_conflicts` を追加（同一 `x_post_url` が複数candidate/fileに紐づく不整合を検出）
    - `--strict-url-mapping` を追加（URL帰属不整合が1件でも終了コード2）
    - Markdown/JSONレポートへ `url_conflicts` を出力
  - 検証実行
    - `python3 -m py_compile scripts/check_human_observations.py scripts/make_candidates.py scripts/check_png_hdr.py`（OK）
    - `python3 scripts/check_human_observations.py --observations docs/human-observations.md --generated-dir generated --report-out docs/observation-status-2026-03-08.md --report-json-out docs/observation-status-2026-03-08.json --post-checklist-out docs/posting-checklist-2026-03-08.md --checklist-batch-mode diversified --checklist-include-targeted-followups --batch-size 10 --batch-family-cap 2 --strict-mapping --strict-url-mapping`（OK, `url_conflicts=0`）
    - `python3 scripts/check_human_observations.py --observations /tmp/url_conflict_test.md --generated-dir generated --strict-url-mapping`（exit=2で検出動作を確認）
  - ドキュメント更新
    - `README.md` に `url_conflicts` / `--strict-url-mapping` を追記
    - `docs/reproduction-candidates.md` を第25版に更新

- 2026-03-08 06:56 JST
  - 実験設計を「観測追記先の誤運用（hypothesis側への混入）検知」に拡張
    - 背景: 人間観測は `docs/human-observations*.md` に集約する運用だが、`docs/hypothesis-update-*.md` 側への追記混入を自動検知できていなかった
  - `scripts/check_human_observations.py` を拡張
    - `forbidden_observation_entries` を追加（禁止ソース内の観測表行/X投稿URLを検出）
    - `--forbid-observation-in-glob` を追加（禁止ソースglobを追加指定）
    - `--no-default-forbidden-observation-sources` を追加（既定 `hypothesis-update-*.md` 走査の無効化）
    - `--strict-forbidden-observation-source` を追加（禁止ソース検出時に終了コード2）
    - Markdown/JSONレポートへ `forbidden_observation_entries` を出力
  - 検証実行
    - `python3 -m py_compile scripts/check_human_observations.py`（OK）
    - `python3 scripts/check_human_observations.py --observations docs/human-observations.md --generated-dir generated --report-out docs/observation-status-2026-03-08.md --report-json-out docs/observation-status-2026-03-08.json --post-checklist-out docs/posting-checklist-2026-03-08.md --checklist-batch-mode diversified --checklist-include-targeted-followups --batch-size 10 --batch-family-cap 2 --strict-mapping --strict-url-mapping`（OK, `forbidden_observation_entries=5`）
    - `python3 scripts/check_human_observations.py --observations docs/human-observations.md --generated-dir generated --strict-forbidden-observation-source`（exit=2で検出動作を確認）
  - ドキュメント更新
    - `README.md` に禁止ソース検出オプションと `--strict-forbidden-observation-source` を追記
    - `docs/reproduction-candidates.md` を第27版に更新

- 2026-03-08 07:05 JST
  - 検証フェーズを一旦停止し、JavaScript/WASM実装フェーズへ移行
  - 最小生成パイプラインを追加
    - `src/jswasm-pipeline/pipeline.mjs`
      - RGBA16 PNG writer（IHDR/iCCP/IDAT/IEND）
      - success PNG から iCCP 抽出
      - WASM `mulDiv255`（利用不可時はJS fallback）
    - `src/jswasm-pipeline/cli.mjs`
      - 最小生成CLI（`candidate_success_like.png` / `candidate_fail_no_iccp.png`）
  - 失敗時フォールバックを追加
    - `scripts/generate_candidates.mjs`
      - まず JS/WASM を実行
      - 失敗時に `scripts/make_candidates.py --extended` へ自動切替
  - 投稿前チェックを1コマンド化
    - `scripts/posting_precheck.mjs`
      - `check_png_hdr.py` + `check_human_observations.py` を順次実行
  - 実行手順を整備
    - `package.json`（`gen:jswasm` / `gen` / `precheck:post`）
    - `README.md` に「JavaScript/WASM 実装フェーズ（最短実行）」を追記
    - `docs/jswasm-quickstart.md` を新規作成

- 2026-03-08 07:24 JST
  - 検証フェーズを停止し、JavaScript/WASM実装フェーズへ移行
  - `src/jswasm-pipeline/pipeline.mjs`
    - PNGチャンク走査の境界チェックを追加（破損PNGでの明示エラー化）
    - width/height/alpha8Patch のクランプを追加（16..4096 / 0..255）
  - `src/jswasm-pipeline/cli.mjs`
    - `--help` 追加
    - 不明引数で即エラーにするパーサへ更新
  - `scripts/generate_candidates.mjs`
    - `--force-fallback` と `HDR_FORCE_PY_FALLBACK=1` を追加
    - JS/WASM失敗時の理由表示を強化
    - `--success-ref` / `--outdir` / `--fallback-input` 対応
  - `scripts/posting_precheck.mjs`
    - 投稿前チェックCLIを1コマンド運用向けに拡張
    - JST当日を自動採番して `docs/observation-status-YYYY-MM-DD.*` / `docs/posting-checklist-YYYY-MM-DD.md` へ出力
    - `--generated-dir` / `--observations` / `--out-date` / batchオプション対応
  - `package.json` に `npm run impl:run`（`gen` + `precheck:post`）を追加
  - `README.md` / `docs/jswasm-quickstart.md` を最短実行手順に合わせて更新
  - 実行確認
    - `npm run gen:jswasm -- --alpha8-patch 48`（OK）
    - `HDR_FORCE_PY_FALLBACK=1 npm run gen`（Pythonフォールバック経路OK）
    - `npm run precheck:post`（OK）
    - `npm run impl:run`（OK）

- 2026-03-08 09:15 JST
  - 現状調査を実施
    - リポジトリ構成、`README.md`、`docs/`、`package.json`、`連絡帳.md` の直近履歴を確認
    - 現在方針が「JavaScript/WASM 実装フェーズ優先」であることを再確認
  - 動作確認
    - `npm run precheck` を実行（OK）
    - `docs/observation-status-2026-03-08.md` / `.json` と `docs/posting-checklist-2026-03-08.md` の出力を確認
  - 生成された知見ドキュメント
    - `docs/current-status-2026-03-08.md` を追加
      - 現在の到達点
      - 注意点（`checked_at` 更新）
      - 未完了項目（実投稿による人手検証、フロントエンド統合導線）

- 2026-03-08 09:21 JST
  - 相談事項「Pythonでは発光するがJS/WASMでは未達」の原因切り分けを実施
    - メタ情報比較では差がなく、`iCCP/cicp` は Python/JSWASM とも一致
    - 画素比較で差を確認:
      - `sample/success_sample.png` は実画像由来
      - 旧JS/WASM `candidate_success_like.png` は中央白パッチの合成ミニマル画像
    - 結論: 失敗要因は `iCCP` ではなく、生成画素が Python版と一致していないこと
  - 実装変更
    - `src/jswasm-pipeline/pipeline.mjs`
      - 既定モードを `pass-through` に拡張
      - `candidate_success_like.png` は入力PNGをそのまま採用
      - `candidate_fail_no_iccp.png` は同一PNGから iCCP だけ除去
      - 従来方式は `--mode minimal-pattern` として維持
    - `src/jswasm-pipeline/core.mjs`
      - `stripIccProfileFromPngBytes` / `upsertIccProfileToPngBytes` を追加
    - `src/jswasm-pipeline/cli.mjs` / `scripts/generate_candidates.mjs`
      - `--input` と `--mode` を追加
  - 検証
    - `npm run gen:jswasm`（OK）
    - `npm run precheck`（OK）
    - `cmp sample/success_sample.png generated/jswasm/candidate_success_like.png`（一致）
    - `python3 scripts/check_png_hdr.py generated/jswasm/candidate_success_like.png`（target profile一致）
    - `python3 scripts/check_png_hdr.py generated/jswasm/candidate_fail_no_iccp.png`（iCCPなし）
  - ドキュメント
    - `docs/jswasm-hdr-outlook-2026-03-08.md` を追加（目処整理）
    - `README.md` / `docs/jswasm-quickstart.md` を新モードに合わせて更新

- 2026-03-08 09:35 JST
  - フィードバック反映: 「ミニマル画像を使う場合は白パッチを画面端に接触させる」
  - `src/jswasm-pipeline/core.mjs`
    - `buildMinimalPatternRgba16` を更新
    - 中央正方形パッチ -> 右端接触の高輝度長方形パッチへ変更
  - ドキュメント更新
    - `README.md`
    - `docs/jswasm-quickstart.md`
  - 動作確認
    - `npm run gen:jswasm -- --mode minimal-pattern --outdir generated/jswasm_minimal_edge`（OK）
    - `python3 scripts/check_png_hdr.py generated/jswasm_minimal_edge/candidate_success_like.png`（RGBA16+iCCP+cicp一致）
    - `python3 scripts/check_png_hdr.py generated/jswasm_minimal_edge/candidate_fail_no_iccp.png`（iCCPなし）

- 2026-03-08 09:37 JST
  - `.DS_Store` を Git 管理対象から外すため、`.gitignore` に `.DS_Store` を追加

- 2026-03-08 09:52 JST
  - 追加画像 `sample/base.png`（1008x756 / 16bit RGB）を、`minimal-pattern` の背景として使えるように JS/WASM パイプラインを拡張
  - `src/jswasm-pipeline/core.mjs`
    - RGBA16復元用のPNGデコーダ（color_type=2/6, bit_depth=16）を追加
    - nearest-neighbor リサイズを追加
    - `buildMinimalPatternRgba16` が背景RGBA16を受け取り、右端接触の白長方形パッチを重畳できるよう更新
  - `src/jswasm-pipeline/pipeline.mjs`
    - `--mode minimal-pattern` 時に `--input` PNG を背景として decode+resize し、白パッチを重畳する経路へ変更
  - 動作確認
    - `npm run gen:jswasm -- --mode minimal-pattern --input sample/base.png --outdir generated/jswasm_minimal_base`（OK）
    - `python3 scripts/check_png_hdr.py generated/jswasm_minimal_base/candidate_success_like.png`（RGBA16+iCCP+cicp一致）
    - `python3 scripts/check_png_hdr.py generated/jswasm_minimal_base/candidate_fail_no_iccp.png`（iCCPなし）
  - ドキュメント更新
    - `README.md` / `docs/jswasm-quickstart.md` に `minimal-pattern` の背景入力仕様を追記

- 2026-03-08 09:54 JST
  - フィードバック反映: `minimal-pattern` の白パッチ既定alphaを `64 -> 255`（不透明）へ変更
  - 変更箇所
    - `src/jswasm-pipeline/cli.mjs` の `--alpha8-patch` 既定値/ヘルプを更新
    - `README.md` / `docs/jswasm-quickstart.md` に既定不透明を追記
  - 動作確認
    - `npm run gen:jswasm -- --mode minimal-pattern --input sample/base.png --outdir generated/jswasm_minimal_base_default`（OK）
    - `python3 scripts/check_png_hdr.py generated/jswasm_minimal_base_default/candidate_success_like.png`（RGBA16+iCCP+cicp一致）

- 2026-03-08 09:57 JST
  - 成果物削除後の再生成を実施
  - 実行
    - `npm run gen:jswasm`（pass-through成果物を再生成）
    - `npm run precheck`（レポート再生成、OK）
    - `npm run gen:jswasm -- --mode minimal-pattern --input sample/base.png --outdir generated/jswasm_minimal_base_default`（minimal-pattern成果物を再生成）
  - 生成確認
    - `generated/jswasm/candidate_success_like.png`
    - `generated/jswasm/candidate_fail_no_iccp.png`
    - `generated/jswasm_minimal_base_default/candidate_success_like.png`
    - `generated/jswasm_minimal_base_default/candidate_fail_no_iccp.png`
- 2026-03-08 09:34 JST
    - 人間による追記
        - `docs/human-observations.md` に実際にXで投稿した結果を追記
