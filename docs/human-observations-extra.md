# Human Observations (Extra)

`docs/human-observations.md` に載せる前のメモ置き場。

- 投稿直後でURL確定前の下書き
- 端末別の一時メモ（後で主テーブルへ転記）

`check_human_observations.py` では、
`--observations-glob 'human-observations-*.md'` を使うとこのファイルも同時読込できる。

| candidate | file | observed | x_post_url | notes |
|---|---|---|---|---|
